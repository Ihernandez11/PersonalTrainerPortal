@model IEnumerable<PersonalTrainerPortal.Models.Data.Client>

@{
    ViewBag.Title = "Clients";
}


<div class="container_styled_1">
    <div class="container margin_60">
        <div class="row">
            <div class="col-md-12">
                <div class="box_style_6">
                    <hr class="styled_2">
                    <div class="indent_title_in">
                        <i class="icon_archive_alt"></i>
                        <h3>My Clients</h3>
                    </div>
                    <div class="wrapper_indent">
                        <div class="table-responsive">
                            
                            <table class="table table-striped">
                                <tr>
                                    <th>
                                        @Html.DisplayNameFor(model => model.Email)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.FirstName)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.LastName)
                                    </th>
                                    <th></th>
                                </tr>

                                @foreach (var client in Model)
                                {
                                    <tr>
                                        <td>
                                            @Html.DisplayFor(modelItem => client.Email)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => client.FirstName)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => client.LastName)
                                        </td>
                                        <td>
                                            @Html.ActionLink("Set Workouts", "Workout", new { UID = client.PersonalTrainerID, CID = client.UserID }) |
                                            @Html.ActionLink("Remove Client", "Remove", new { UID = client.PersonalTrainerID, CID = client.UserID })
                                        </td>
                                    </tr>
                                }

                            </table>
                        </div>
                    </div><!--wrapper_indent -->
                    
                </div>
            </div>

        </div><!--End row -->
    </div><!--End container -->
</div>




